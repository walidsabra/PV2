@model IEnumerable<ProductVisualizer.Models.MarketSector>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MarketSectorName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MarketSectorName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MarketSectorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MarketSectorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MarketSectorId })
        </td>
    </tr>
}

</table>
