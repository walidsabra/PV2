@model IEnumerable<ProductVisualizer.Models.BIMUse>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BIMUseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BIMUseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BIMUseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BIMUseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BIMUseId })
        </td>
    </tr>
}

</table>
